import themeSettings from '../../themes/ttg';

const Core = {
  buttonSettings: themeSettings.theme.buttons,
  fontSettings: themeSettings.theme.font,
  colorSettings: themeSettings.theme.colors,
  layoutSettings: themeSettings.theme.layout,
  globalSettings: themeSettings.theme.global,
};

module.exports = Core; // eslint-disable-line no-undef
